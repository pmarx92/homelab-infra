apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: homelab-prometheus
    access: proxy
    url: http://host.docker.internal:9090
    isDefault: false
    editable: true
    jsonData:
      timeInterval: "15s"
      httpMethod: POST
